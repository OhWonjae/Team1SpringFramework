<?xml version="1.0" encoding="euc-kr" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.webapp.dao.ProductDao">



   <select id="selectAllBypid" resultType="size">
   	select p_size
   	from SizeProduct
   	order by p_size asc
   </select>
   
	<insert id="insert" parameterType="size">
		insert into size_products
		(photo_id, p_id, photo_oname, photo_sname, photo_type, photo_role)
		values
		(#{photo_id}, #{p_id}, #{photo_oname}, #{photo_sname}, #{photo_type}, #{photo_role})
	</insert>
</mapper>